QueueFromDequeImpl.java
	Exercise1- Implement a Queue with a Deque
	Uses methods created in the Deque class and creates methods for a queue.

ArithmeticExpressionEvaluator.java
	Exercise 2- Evaluation of Arithmetic Expressions
	Takes in a string arithmetic expression and uses stacks to evaluate the 		expression based on the parentheses.
DoubleStackImpl.java
	Exercise 4- Shared memory
	Uses the double stack interface to create two stacks in the same array, such that	each can grow independently, but neither can grow if the array is full.
AgesOfHollywood.java
	Exercise 6- Queuing Stars
	Reads a text file and sorts the information about each celebrity to create a queue
	that displays the celebrities in order of birth. 